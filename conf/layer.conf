# We have a conf and classes directory, add to BBPATH
BBPATH .= ":${LAYERDIR}"

# We have recipes-* directories, add to BBFILES
BBFILES += "${LAYERDIR}/recipes-*/*/*.bb \
            ${LAYERDIR}/recipes-*/*/*.bbappend"

BBFILE_COLLECTIONS += "meta-adlink-demo"
BBFILE_PATTERN_meta-adlink-demo = "^${LAYERDIR}/"
BBFILE_PRIORITY_meta-adlink-demo = "6"

LAYERDEPENDS_meta-adlink-demo = "core"
LAYERSERIES_COMPAT_meta-adlink-demo = "kirkstone mickledore scarthgap"
